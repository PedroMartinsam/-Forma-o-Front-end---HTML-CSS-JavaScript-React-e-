# 🧩 Projeto - Arrays e Objetos em JavaScript

Este projeto tem como objetivo demonstrar, na prática, o uso de **arrays, objetos e seus principais métodos em JavaScript**, além de conceitos como manipulação de dados, loops, mutação, destructuring, rest operator e JSON.

Projeto fundamental para quem está evoluindo na **lógica de programação e estrutura de dados em JavaScript**.

---

## ✅ Conteúdos Abordados

### 📌 Arrays
- Criação de arrays
- Acesso por índice
- Propriedade `length`
- Métodos:
  - `concat()`
  - `push()` e `pop()`
  - `shift()` e `unshift()`
  - `indexOf()` e `lastIndexOf()`
  - `slice()`
  - `forEach()`
  - `includes()`
  - `reverse()`

### 📌 Strings
- `trim()`
- `padStart()` e `padEnd()`
- `split()`
- `join()`
- `repeat()`

### 📌 Objetos
- Criação e acesso a propriedades
- Adição e remoção de propriedades
- `Object.assign()`
- `Object.keys()`
- `Object.entries()`
- Mutação de objetos
- `instanceof`

### 📌 Outros Conceitos
- Loop em arrays
- Rest Operator (`...`)
- `for...of`
- Destructuring de objetos e arrays
- JSON (`JSON.parse()` e `JSON.stringify()`)

---

## 🛠️ Tecnologias Utilizadas

- HTML5  
- JavaScript  

---

## 📂 Estrutura do Projeto

