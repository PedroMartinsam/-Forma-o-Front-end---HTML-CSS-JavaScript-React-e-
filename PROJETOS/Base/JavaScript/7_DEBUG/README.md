#  Debug em JavaScript – Tratamento de Erros e Boas Práticas

Este projeto foi desenvolvido com foco no aprendizado de **depuração (debug)** em JavaScript, abordando desde o uso do `"use strict"` até técnicas de **tratamento de erros, exceções, validação de dados, try/catch, finally e assertions**.

Ideal para quem está construindo uma base sólida na linguagem.

---

## 🚀 Conteúdos Abordados

✅ Modo `"use strict"`  
✅ Uso do `console.log` para debug  
✅ Uso do `debugger`  
✅ Conversão e validação de dados com `Number()`  
✅ Verificação de valores inválidos (`NaN`)  
✅ Lançamento de erros com `throw new Error()`  
✅ Tratamento de exceções com `try...catch`  
✅ Uso do `finally`  
✅ Criação de função de validação de arrays (assertion)  

---

## 🛠️ Tecnologias Utilizadas

- HTML5  
- JavaScript (ES6+)

---

## 📂 Estrutura do Projeto

```bash
/
├── index.html
└── js/
    └── scripts.js
