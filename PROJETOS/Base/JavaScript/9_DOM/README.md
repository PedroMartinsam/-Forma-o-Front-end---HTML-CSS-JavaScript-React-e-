# 🧠 Manipulação do DOM com JavaScript

Projeto criado para praticar a **manipulação do DOM (Document Object Model)** utilizando JavaScript puro. Aqui são trabalhados conceitos essenciais para desenvolvimento Front-end moderno.

Neste projeto são utilizados métodos de seleção, criação, alteração de elementos, estilos via JS e manipulação de atributos.

---

## 🚀 Conteúdos Abordados

✅ Navegação pela árvore do DOM  
✅ `getElementsByTagName`  
✅ `getElementById`  
✅ `getElementsByClassName`  
✅ `querySelector` e `querySelectorAll`  
✅ `createElement`  
✅ `createTextNode`  
✅ `appendChild`  
✅ `insertBefore`  
✅ `replaceChild`  
✅ Manipulação de atributos (`setAttribute`, `getAttribute`)  
✅ Estilização via JavaScript (`style`)  
✅ Manipulação de múltiplos elementos com `for...of`  
✅ Medidas de elementos (`offsetWidth`, `clientHeight`)  
✅ Posição de elementos (`getBoundingClientRect`)

---

## 🛠️ Tecnologias Utilizadas

- HTML5  
- CSS3  
- JavaScript (Vanilla JS)

---

## 📂 Estrutura do Projeto

```bash
/
├── index.html
├── css/
│   └── styles.css
└── js/
    └── scripts.js
