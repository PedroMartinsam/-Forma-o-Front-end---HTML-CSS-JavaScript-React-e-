# 📌 Projeto - Estudos de Eventos em JavaScript

Este projeto tem como objetivo demonstrar, de forma prática, o funcionamento dos principais **eventos em JavaScript**, incluindo clique, teclado, mouse, foco, scroll, carregamento da página, propagação de eventos e prevenção de comportamento padrão.

Ideal para quem está aprendendo **DOM, Event Listeners e Interações com o Usuário**.

---

## 🚀 Funcionalidades Demonstradas

- ✅ Adicionar eventos com `addEventListener`
- ✅ Remover eventos dinamicamente
- ✅ Captura de argumentos do evento (`event`)
- ✅ Controle de propagação com `stopPropagation()`
- ✅ Prevenção do comportamento padrão (`preventDefault()`)
- ✅ Eventos de teclado (`keydown` e `keyup`)
- ✅ Eventos do mouse (clique, duplo clique, pressionar)
- ✅ Movimento do mouse
- ✅ Evento de scroll da página
- ✅ Eventos de foco (`focus` e `blur`)
- ✅ Eventos de carregamento da página (`load` e `beforeunload`)

---

## 🛠️ Tecnologias Utilizadas

- HTML5  
- CSS3  
- JavaScript (Vanilla JS)

---

## 📂 Estrutura do Projeto

