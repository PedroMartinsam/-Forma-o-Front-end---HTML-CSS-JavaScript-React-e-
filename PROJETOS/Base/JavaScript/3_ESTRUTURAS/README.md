# 🧠 Projeto - Estruturas de Programação em JavaScript

Este projeto tem como objetivo demonstrar, na prática, as **principais estruturas de programação em JavaScript**, incluindo variáveis, condicionais, laços de repetição, funções do objeto Math, interação com usuário, controle de fluxo e estruturas `switch`.

Projeto essencial para quem está aprendendo **lógica de programação com JavaScript**.

---

## ✅ Conteúdos Abordados

- 🔹 Declaração de variáveis (`let` e `const`)
- 🔹 Tipagem dinâmica com `typeof`
- 🔹 Boas práticas de nomenclatura de variáveis
- 🔹 Entrada de dados com `prompt`
- 🔹 Saída de dados com `alert`
- 🔹 Funções do objeto `Math`:
  - `Math.max()`
  - `Math.floor()`
  - `Math.ceil()`
- 🔹 Uso do `console.log`, `console.error` e `console.warn`
- 🔹 Estruturas condicionais:
  - `if`
  - `else`
  - `else if`
- 🔹 Laços de repetição:
  - `while`
  - `do while`
  - `for`
- 🔹 Controle de fluxo:
  - `break`
  - `continue`
- 🔹 Estrutura `switch case`
- 🔹 Conceito de identação e blocos de código

---

## 🛠️ Tecnologias Utilizadas

- HTML5  
- JavaScript  

---

## 📂 Estrutura do Projeto

